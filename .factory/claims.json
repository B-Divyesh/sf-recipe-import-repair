[
  {
    "id": "format-import",
    "claim": "Reads Recipe JSON, JSON-LD, and Markdown into editable fields",
    "where": "landing workbench, README",
    "test": "npm test -- --grep @claim:format-import",
    "sandbox": "fresh browser context; paste JSON, JSON-LD, and Markdown fixtures separately and assert each parsed title and source format"
  },
  {
    "id": "reversible-repairs",
    "claim": "Every suggested repair can be undone",
    "where": "landing method, demo toolbar, README",
    "test": "npm test -- --grep @claim:reversible-repairs",
    "sandbox": "demo data; apply each of the three suggested repairs separately, assert its exact result, undo, and assert its original value"
  },
  {
    "id": "neutral-export",
    "claim": "Exports neutral JSON and preserves source attribution",
    "where": "landing method, export toolbar, README",
    "test": "npm test -- --grep @claim:neutral-export",
    "sandbox": "demo data; apply all safe repairs, download JSON, and assert schema, repaired ingredient, source URL, and author"
  },
  {
    "id": "local-only",
    "claim": "No recipe text leaves your device and the repair flow makes no third-party requests",
    "where": "landing facts and privacy page, README",
    "test": "npm test -- --grep @claim:local-only",
    "sandbox": "fresh browser context; record every request during the demo repair flow and assert every URL is same-origin"
  },
  {
    "id": "demo-isolation",
    "claim": "Demo source uses a separate demo: session key and never enters real storage",
    "where": "demo banner, privacy page, README",
    "test": "npm test -- --grep @claim:demo-isolation",
    "sandbox": "fresh browser context; mutate demo, assert localStorage is empty and only a demo-prefixed session key exists, then leave demo and assert it is removed"
  },
  {
    "id": "offline-reload",
    "claim": "Works offline after the first visit",
    "where": "landing facts, README",
    "test": "npm test -- --grep @claim:offline-reload",
    "sandbox": "fresh browser context; load demo, wait for service worker control, switch the browser offline, reload, and assert the sample bench is available"
  },
  {
    "id": "file-limit",
    "claim": "Rejects recipe files larger than 2 MB",
    "where": "workbench input note",
    "test": "npm test -- --grep @claim:file-limit",
    "sandbox": "fresh browser context; choose a generated 2 MB plus one byte fixture and assert the size error"
  },
  {
    "id": "free-flow",
    "claim": "The full repair and export flow is free and needs no account",
    "where": "landing facts and privacy section, README",
    "test": "npm test -- --grep @claim:free-flow",
    "sandbox": "fresh browser context; repair the demo and reach enabled export with no sign-in, purchase, or payment step"
  },
  {
    "id": "source-url-no-fetch",
    "claim": "Web addresses are preserved in neutral exports and never fetched",
    "where": "workbench input note, landing limits, README",
    "test": "npm test -- --grep @claim:source-url-no-fetch",
    "sandbox": "fresh browser context; repair and export the demo source URL while recording requests, then assert attribution is retained and no request reaches its URL origin"
  },
  {
    "id": "instructions-unchanged",
    "claim": "Repairs do not change cooking instructions",
    "where": "landing limits, README",
    "test": "npm test -- --grep @claim:instructions-unchanged",
    "sandbox": "fresh browser context; record every demo instruction, apply all suggested repairs, and assert every instruction is byte-for-byte unchanged"
  },
  {
    "id": "repair-diagnostics",
    "claim": "Points to malformed quantities, verbose units, missing data, invalid source addresses, and oversized fields",
    "where": "README",
    "test": "npm test -- --grep @claim:repair-diagnostics",
    "sandbox": "fresh browser context; inspect deterministic demo and JSON fixtures, then blank an edited title and ingredient to assert named diagnostics and disabled export"
  },
  {
    "id": "exact-change-preview",
    "claim": "Every suggested repair shows its exact before and after value",
    "where": "landing method, README",
    "test": "npm test -- --grep @claim:exact-change-preview",
    "sandbox": "fresh browser context; open each demo repair preview and assert its exact Before and After values"
  }
]
